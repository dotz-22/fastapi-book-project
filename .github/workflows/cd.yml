# name: Deploy Application

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Deploy
#         run: |
#           ssh user@server-ip "cd /path/to/app && git pull && systemctl restart fastapi"